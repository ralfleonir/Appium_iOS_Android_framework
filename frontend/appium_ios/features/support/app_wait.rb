# This class is not functional yet. It's no working on iOS
class AppWait

  def waitPresence (element)
    # wait = Selenium::WebDriver::Wait.new(:timeout => 0)
    # wait.until { find_element(:id, element) }
  end

end
